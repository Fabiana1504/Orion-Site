/**
 * Rotación 360° “real” (varias vistas del coche).
 *
 * 1) Exportá renders del mismo coche cada ~10° (36 fotos) o cada 15° (24).
 * 2) Guardalas en `public/images/car-360/` con nombres consecutivos.
 * 3) Listá las rutas abajo en orden (0° → 360°).
 *
 * Dejá el array vacío para usar solo `/images/hero-car.png` con giro 3D CSS.
 */
export const CAR_360_FRAMES = [
  // Ejemplo (descomentá y renombrá archivos reales):
  // "/images/car-360/01.png",
  // "/images/car-360/02.png",
  // ... hasta completar la vuelta
];
